import { HamburguerToggleDirective } from './hamburguer-toggle.directive';

describe('HamburguerToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new HamburguerToggleDirective();
    expect(directive).toBeTruthy();
  });
});
